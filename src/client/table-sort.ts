type SortDirection="ascending"|"descending";
function sortableValue(cell:HTMLTableCellElement){const raw=(cell.dataset.sortValue??cell.textContent??"").replace(/\s+/g," ").trim();if(!raw)return{kind:0,value:""};const date=Date.parse(raw.replace(/\./g,"-").replace(/오전\s*/,"AM ").replace(/오후\s*/,"PM "));if(/\d{4}[.\-/]\s*\d{1,2}[.\-/]\s*\d{1,2}/.test(raw)&&Number.isFinite(date))return{kind:2,value:date};const numeric=raw.replace(/,/g,"").match(/^[-+]?\d+(?:\.\d+)?(?:\s*(?:건|개|명|회|원|%))?$/);if(numeric)return{kind:2,value:Number.parseFloat(numeric[0])};return{kind:1,value:raw}}
function compareCells(left:HTMLTableCellElement|undefined,right:HTMLTableCellElement|undefined){const a=left?sortableValue(left):{kind:0,value:""},b=right?sortableValue(right):{kind:0,value:""};if(a.kind!==b.kind)return a.kind-b.kind;if(typeof a.value==="number"&&typeof b.value==="number")return a.value-b.value;return String(a.value).localeCompare(String(b.value),"ko",{numeric:true,sensitivity:"base"})}
document.addEventListener("click",event=>{const target=event.target as Element,header=target.closest<HTMLTableCellElement>("table thead th");if(!header||header.colSpan>1||header.dataset.noSort!==undefined)return;if(target.closest("input,select,textarea,a")||header.dataset.sort||header.querySelector("[data-sort]"))return;const table=header.closest<HTMLTableElement>("table");if(!table)return;const index=[...header.parentElement!.children].indexOf(header);if(table.matches(".shrine-grid")&&index<7)return;const body=table.tBodies[0];if(!body||body.rows.length<2)return;const direction:SortDirection=header.getAttribute("aria-sort")==="ascending"?"descending":"ascending";table.querySelectorAll<HTMLTableCellElement>("thead th").forEach(cell=>{cell.removeAttribute("aria-sort");cell.classList.remove("table-sort-asc","table-sort-desc")});header.setAttribute("aria-sort",direction);header.classList.add(direction==="ascending"?"table-sort-asc":"table-sort-desc");const rows=[...body.rows].map((row,position)=>({row,position}));rows.sort((a,b)=>{const result=compareCells(a.row.cells[index],b.row.cells[index]);return(result||a.position-b.position)*(direction==="ascending"?1:-1)});rows.forEach(item=>body.append(item.row))});
document.head.insertAdjacentHTML("beforeend",'<style>table thead th:not([data-no-sort]){cursor:pointer;user-select:none}table thead th.table-sort-asc:after,table thead th.table-sort-desc:after{display:inline-block;margin-left:6px;color:currentColor;font-size:9px}table thead th.table-sort-asc:after{content:"▲"}table thead th.table-sort-desc:after{content:"▼"}</style>');
