function confirmAdminMissionAnswer(){return new Promise<boolean>(resolve=>{document.querySelector("#mission-answer-confirm-modal")?.remove();document.body.insertAdjacentHTML("beforeend",'<div id="mission-answer-confirm-modal" class="priest-modal"><div class="priest-modal-backdrop" data-admin-answer-confirm="false"></div><section class="priest-modal-box mission-answer-confirm-box"><header><div><p>MISSION Q&amp;A</p><h2>답변 등록</h2></div></header><div class="mission-answer-confirm-body">답장을 보내시겠습니까</div><footer><button class="secondary" type="button" data-admin-answer-confirm="false">취소</button><button class="primary" type="button" data-admin-answer-confirm="true">확인</button></footer></section></div>');document.querySelectorAll<HTMLButtonElement>("[data-admin-answer-confirm]").forEach(button=>button.onclick=()=>{document.querySelector("#mission-answer-confirm-modal")?.remove();resolve(button.dataset.adminAnswerConfirm==="true")})})}
function promptAdminApplicationRejection(){return new Promise<string|null>(resolve=>{document.querySelector("#application-reject-modal")?.remove();document.body.insertAdjacentHTML("beforeend",'<div id="application-reject-modal" class="priest-modal"><div class="priest-modal-backdrop" data-application-reject-cancel></div><section class="priest-modal-box application-reject-box"><header><div><p>MISSION APPLICATION</p><h2>미션 지원 반려</h2></div></header><div class="application-reject-body"><label>반려 사유<textarea maxlength="1000" rows="6" placeholder="지원자에게 전달할 반려 사유를 작성해 주세요."></textarea></label><p></p></div><footer><button class="secondary" type="button" data-application-reject-cancel>취소</button><button class="primary" type="button" data-application-reject-submit disabled>반려</button></footer></section></div>');const modal=document.querySelector("#application-reject-modal")!,textarea=modal.querySelector<HTMLTextAreaElement>("textarea")!,submit=modal.querySelector<HTMLButtonElement>("[data-application-reject-submit]")!;textarea.oninput=()=>submit.disabled=!textarea.value.trim();modal.querySelectorAll<HTMLElement>("[data-application-reject-cancel]").forEach(element=>element.onclick=()=>{modal.remove();resolve(null)});submit.onclick=()=>{const reason=textarea.value.trim();if(!reason)return;modal.remove();resolve(reason)}})}
function confirmAdminApplicationApproval(){return new Promise<boolean>(resolve=>{document.querySelector("#application-approval-confirm-modal")?.remove();document.body.insertAdjacentHTML("beforeend",'<div id="application-approval-confirm-modal" class="priest-modal"><div class="priest-modal-backdrop" data-application-approval-confirm="false"></div><section class="priest-modal-box application-approval-confirm-box"><header><div><p>MISSION APPLICATION</p><h2>미션 지원 승락</h2></div></header><div class="application-approval-confirm-body">승락했다는 알림을 전송합니다</div><footer><button class="secondary" type="button" data-application-approval-confirm="false">취소</button><button class="primary" type="button" data-application-approval-confirm="true">확인</button></footer></section></div>');const modal=document.querySelector("#application-approval-confirm-modal")!;modal.querySelectorAll<HTMLButtonElement>("[data-application-approval-confirm]").forEach(button=>button.onclick=()=>{modal.remove();resolve(button.dataset.applicationApprovalConfirm==="true")})})}
document.addEventListener("click",event=>{const button=(event.target as Element).closest<HTMLButtonElement>('[data-admin-application][data-status="approved"]');if(!button||button.dataset.approvalConfirmed==="true")return;event.preventDefault();event.stopImmediatePropagation();void confirmAdminApplicationApproval().then(confirmed=>{if(!confirmed)return;button.dataset.approvalConfirmed="true";button.click();queueMicrotask(()=>delete button.dataset.approvalConfirmed)})},true);
function prepareAdminAnswerForms(){document.querySelectorAll<HTMLFormElement>("[data-admin-answer]").forEach(form=>{if(form.dataset.answerReady)return;form.dataset.answerReady="true";const textarea=form.querySelector<HTMLTextAreaElement>("textarea"),submit=form.querySelector<HTMLButtonElement>('button[type="submit"]');if(!textarea||!submit)return;const sync=()=>submit.disabled=!textarea.value.trim();textarea.addEventListener("input",sync);sync()})}
new MutationObserver(prepareAdminAnswerForms).observe(document.documentElement,{childList:true,subtree:true});
document.addEventListener("submit",event=>{const form=event.target as HTMLFormElement;if(!form.matches("[data-admin-answer]")||form.dataset.answerConfirmed==="true")return;event.preventDefault();event.stopImmediatePropagation();const textarea=form.querySelector<HTMLTextAreaElement>("textarea");if(!textarea?.value.trim())return;void confirmAdminMissionAnswer().then(confirmed=>{if(!confirmed)return;form.dataset.answerConfirmed="true";form.requestSubmit();queueMicrotask(()=>delete form.dataset.answerConfirmed)})},true);

type MissionStatus="requested"|"approved"|"rejected"|"ended";
type Mission={id:number;title:string;content:string;tags:string[];anonymous:boolean;status:MissionStatus;rejectionReason:string|null;authorName:string;baptismalName:string|null;applicationFrom:string|null;applicationTo:string|null;applicationOpen:boolean;applicationCount:number;approvedApplicationCount:number;rejectedApplicationCount:number;createdAt:string;decidedAt:string|null};
const labels={requested:"승인신청",approved:"승인",rejected:"반려"};
(labels as Record<string,string>).ended="종료";
function esc(value:string){const div=document.createElement("div");div.textContent=value;return div.innerHTML}
async function api<T>(url:string,options?:RequestInit){let body=options?.body;if(url==="/api/parish/missions"&&options?.method==="POST"&&typeof body==="string"){const payload=JSON.parse(body);payload.applicationFrom=document.querySelector<HTMLInputElement>("#mission-create-from")?.value??"";payload.applicationTo=document.querySelector<HTMLInputElement>("#mission-create-to")?.value??"";body=JSON.stringify(payload)}const response=await fetch(url,{...options,body,headers:{"Content-Type":"application/json",...(options?.headers??{})}}),data=await response.json() as T&{message?:string};if(!response.ok)throw new Error(data.message??"요청을 처리하지 못했습니다.");return data}
document.querySelector<HTMLButtonElement>('[data-sharing-view="mission"]')?.addEventListener("click",()=>void load());
const missionHeader=document.querySelector<HTMLElement>('.mission-admin>header');
if(missionHeader&&!document.querySelector("#mission-admin-reload")){const actions=document.createElement("div");actions.className="mission-admin-head-actions";const count=document.querySelector("#mission-admin-count")!;missionHeader.append(actions);actions.append(count);actions.insertAdjacentHTML("beforeend",'<div class="mission-admin-search"><input id="mission-admin-author-filter" type="search" placeholder="작성자" aria-label="작성자 조회"><input id="mission-admin-title-filter" type="search" placeholder="제목" aria-label="제목 조회"><select id="mission-admin-status-filter" aria-label="미션 상태 조회"><option value="all">전체 상태</option><option value="approved">승인</option><option value="rejected">반려</option><option value="ended">종료</option><option value="requested">승인대기</option></select><button id="mission-admin-query" class="primary" type="button">조회</button><button id="mission-admin-reset" class="secondary" type="button">초기화</button></div><button id="mission-admin-reload" class="grid-reload-button" type="button"><span aria-hidden="true">↻</span> Reload</button>');const query=()=>applyMissionStatusFilter();document.querySelector<HTMLButtonElement>("#mission-admin-query")!.onclick=query;document.querySelectorAll<HTMLInputElement>("#mission-admin-author-filter,#mission-admin-title-filter").forEach(input=>input.onkeydown=event=>{if(event.key==="Enter")query()});document.querySelector<HTMLButtonElement>("#mission-admin-reset")!.onclick=()=>{document.querySelectorAll<HTMLInputElement>("#mission-admin-author-filter,#mission-admin-title-filter").forEach(input=>input.value="");document.querySelector<HTMLSelectElement>("#mission-admin-status-filter")!.value="all";applyMissionStatusFilter()};document.querySelector<HTMLButtonElement>("#mission-admin-reload")!.onclick=async event=>{const button=event.currentTarget as HTMLButtonElement;button.disabled=true;button.classList.add("loading");try{await load()}finally{window.setTimeout(()=>{button.disabled=false;button.classList.remove("loading")},350)}}}
function applyMissionStatusFilter(){const value=document.querySelector<HTMLSelectElement>("#mission-admin-status-filter")?.value??"all",authorQuery=document.querySelector<HTMLInputElement>("#mission-admin-author-filter")?.value.trim().toLocaleLowerCase("ko-KR")??"",titleQuery=document.querySelector<HTMLInputElement>("#mission-admin-title-filter")?.value.trim().toLocaleLowerCase("ko-KR")??"",cards=[...document.querySelectorAll<HTMLElement>("#mission-admin-list .mission-admin-card")];cards.forEach(card=>{const badge=card.querySelector<HTMLElement>(".mission-status"),status=badge?.classList.contains("requested")?"requested":badge?.classList.contains("approved")?"approved":badge?.classList.contains("rejected")?"rejected":badge?.classList.contains("ended")?"ended":"",title=card.querySelector<HTMLElement>("header strong")?.textContent?.toLocaleLowerCase("ko-KR")??"",author=card.querySelector<HTMLElement>("dl dd")?.textContent?.toLocaleLowerCase("ko-KR")??"",hidden=(value!=="all"&&status!==value)||(Boolean(authorQuery)&&!author.includes(authorQuery))||(Boolean(titleQuery)&&!title.includes(titleQuery));if(card.hidden!==hidden)card.hidden=hidden});const visibleCards=cards.filter(card=>!card.hidden),visible=visibleCards.length,empty=document.querySelector<HTMLElement>("#mission-admin-empty")!;document.querySelector("#mission-admin-count")!.textContent=`조회 ${visible}개 · 승인대기 ${visibleCards.filter(card=>card.querySelector(".mission-status.requested")).length}개`;if(empty.hidden!==(visible>0))empty.hidden=visible>0}
new MutationObserver(()=>{document.querySelectorAll<HTMLButtonElement>(".mission-applicant-count").forEach(applicants=>{const card=applicants.closest<HTMLElement>(".mission-admin-card");if(!card||card.querySelector("[data-admin-questions]"))return;const button=document.createElement("button"),missionId=Number(applicants.dataset.applicants),title=card.querySelector("header strong")?.textContent??"미션";button.type="button";button.className="mission-question-count";button.dataset.adminQuestions=String(missionId);button.textContent="답변/질문 : 0 / 0";button.onclick=()=>openAdminQuestions(missionId,title);applicants.insertAdjacentElement("afterend",button);void api<{items:Array<{answer:string|null}>}>(`/api/parish/missions/${missionId}/questions`).then(data=>button.textContent=`답변/질문 : ${data.items.filter(item=>Boolean(item.answer)).length} / ${data.items.length}`).catch(()=>{button.textContent="답변/질문 확인"})})}).observe(document.body,{childList:true,subtree:true});
async function load(){try{const items=await api<Mission[]>("/api/parish/missions"),list=document.querySelector("#mission-admin-list")!;list.innerHTML=items.map(item=>`<article class="mission-admin-card"><header><div><b class="mission-status ${item.status}">${labels[item.status]}</b><strong>${esc(item.title)}</strong></div><time>${new Date(item.createdAt).toLocaleString("ko-KR")}</time></header><dl><dt>작성자</dt><dd>${esc(item.authorName)}${item.baptismalName?` (${esc(item.baptismalName)})`:""}${item.anonymous?' <small>공개 시 익명</small>':""}</dd><dt>내용</dt><dd>${esc(item.content)}</dd><dt>모집기간</dt><dd>${item.applicationFrom??"-"} ~ ${item.applicationTo??"-"} <b class="mission-open-state ${item.applicationOpen?"open":"closed"}">${item.applicationOpen?"모집중":"비활성"}</b></dd><dt>태그</dt><dd class="mission-tags">${item.tags.length?item.tags.map(tag=>`<span>#${esc(tag)}</span>`).join(""):"-"}</dd><dt>지원자</dt><dd><button class="mission-applicant-count" data-applicants="${item.id}" data-title="${esc(item.title)}" type="button">승인/반려/지원 : ${item.approvedApplicationCount} / ${item.rejectedApplicationCount} / ${item.applicationCount}</button></dd>${item.rejectionReason?`<dt>반려 사유</dt><dd class="mission-reason">${esc(item.rejectionReason)}</dd>`:""}</dl>${item.status==="requested"?`<footer><button class="secondary" data-reject="${item.id}" type="button">반려</button><button class="primary" data-approve="${item.id}" type="button">승인</button></footer>`:""}</article>`).join("");document.querySelector("#mission-admin-count")!.textContent=`총 ${items.length}개 · 승인대기 ${items.filter(item=>item.status==="requested").length}개`;document.querySelector<HTMLElement>("#mission-admin-empty")!.hidden=items.length>0;list.querySelectorAll<HTMLButtonElement>("[data-approve]").forEach(button=>button.onclick=()=>decide(Number(button.dataset.approve),"approved"));list.querySelectorAll<HTMLButtonElement>("[data-reject]").forEach(button=>button.onclick=()=>openReject(Number(button.dataset.reject)));list.querySelectorAll<HTMLButtonElement>("[data-applicants]").forEach(button=>button.onclick=()=>openApplicants(Number(button.dataset.applicants),button.dataset.title??"미션"))}catch(error){window.dispatchEvent(new CustomEvent("parish:notice",{detail:(error as Error).message}))}}
async function decide(id:number,status:"approved"|"rejected",rejectionReason=""){try{const result=await api<{message:string}>(`/api/parish/missions/${id}/decision`,{method:"PATCH",body:JSON.stringify({status,rejectionReason})});document.querySelector("#mission-reject-modal")?.remove();document.body.classList.remove("modal-open");await load();window.dispatchEvent(new CustomEvent("parish:notice",{detail:result.message}))}catch(error){const target=document.querySelector("#mission-reject-error");if(target)target.textContent=(error as Error).message;else window.dispatchEvent(new CustomEvent("parish:notice",{detail:(error as Error).message}))}}
function openReject(id:number){document.querySelector("#mission-reject-modal")?.remove();document.body.insertAdjacentHTML("beforeend",`<div id="mission-reject-modal" class="priest-modal"><div class="priest-modal-backdrop" data-mission-reject-close></div><section class="priest-modal-box mission-reject-box" role="dialog" aria-modal="true"><header><div><p>MISSION REVIEW</p><h2>미션 반려</h2></div><button type="button" data-mission-reject-close>×</button></header><div class="mission-reject-body"><label>반려 사유 <i>*</i><textarea id="mission-reject-reason" maxlength="1000" rows="6" placeholder="작성자에게 전달할 반려 사유를 입력해 주세요."></textarea></label><p id="mission-reject-error"></p></div><footer><button class="secondary" data-mission-reject-close type="button">취소</button><button id="mission-reject-submit" class="primary" type="button" disabled>반려 확정</button></footer></section></div>`);document.body.classList.add("modal-open");const close=()=>{document.querySelector("#mission-reject-modal")?.remove();document.body.classList.remove("modal-open")};document.querySelectorAll("[data-mission-reject-close]").forEach(el=>el.addEventListener("click",close));const reason=document.querySelector<HTMLTextAreaElement>("#mission-reject-reason")!,submit=document.querySelector<HTMLButtonElement>("#mission-reject-submit")!;reason.addEventListener("input",()=>submit.disabled=!reason.value.trim());submit.onclick=()=>{const value=reason.value.trim();if(!value)return;submit.disabled=true;void decide(id,"rejected",value)}}
function openCreate(){document.querySelector("#mission-create-modal")?.remove();document.body.insertAdjacentHTML("beforeend",`<div id="mission-create-modal" class="priest-modal"><div class="priest-modal-backdrop" data-mission-create-close></div><section class="priest-modal-box mission-create-box" role="dialog" aria-modal="true"><header><div><p>PARISH MISSION</p><h2>미션 등록</h2><small>성당명으로 등록되며 즉시 승인·공개됩니다.</small></div><button type="button" data-mission-create-close>×</button></header><form id="mission-create-form"><div class="mission-create-body"><label>제목 <i>*</i><input id="mission-create-title" maxlength="200" required placeholder="미션 제목을 입력해 주세요"></label><label>내용 <i>*</i><textarea id="mission-create-content" maxlength="20000" rows="7" required placeholder="할일과 필요한 역량에 대해 적어주세요"></textarea></label><label>태그<input id="mission-create-tags" maxlength="1000" placeholder="쉼표(,) 또는 띄어쓰기로 구분"></label><div id="mission-create-tag-preview" class="mission-tags"></div><small>쉼표 또는 공백을 입력하면 태그별로 구분됩니다.</small><p id="mission-create-error"></p></div><footer><button class="secondary" data-mission-create-close type="button">취소</button><button class="primary" type="submit">등록 및 공개</button></footer></form></section></div>`);document.body.classList.add("modal-open");const close=()=>{document.querySelector("#mission-create-modal")?.remove();document.body.classList.remove("modal-open")};document.querySelectorAll("[data-mission-create-close]").forEach(el=>el.addEventListener("click",close));const tags=document.querySelector<HTMLInputElement>("#mission-create-tags")!;tags.oninput=()=>{const values=[...new Set(tags.value.split(/[,\s]+/).map(value=>value.replace(/^#/,"").trim()).filter(Boolean))];document.querySelector("#mission-create-tag-preview")!.innerHTML=values.map(value=>`<span>#${esc(value)}</span>`).join("")};document.querySelector<HTMLFormElement>("#mission-create-form")!.onsubmit=async event=>{event.preventDefault();const error=document.querySelector("#mission-create-error")!;error.textContent="";try{const result=await api<{message:string}>("/api/parish/missions",{method:"POST",body:JSON.stringify({title:document.querySelector<HTMLInputElement>("#mission-create-title")!.value,content:document.querySelector<HTMLTextAreaElement>("#mission-create-content")!.value,tags:tags.value})});close();await load();window.dispatchEvent(new CustomEvent("parish:notice",{detail:result.message}))}catch(reason){error.textContent=(reason as Error).message}}}
new MutationObserver(()=>{const tags=document.querySelector<HTMLInputElement>("#mission-create-tags");if(!tags||document.querySelector("#mission-create-from"))return;tags.closest("label")!.insertAdjacentHTML("beforebegin",'<div class="mission-create-period"><label>달란트 모집 시작일 <i>*</i><input id="mission-create-from" type="date" required></label><span>~</span><label>달란트 모집 종료일 <i>*</i><input id="mission-create-to" type="date" required></label></div>');const form=document.querySelector<HTMLFormElement>("#mission-create-form")!,title=document.querySelector<HTMLInputElement>("#mission-create-title")!,content=document.querySelector<HTMLTextAreaElement>("#mission-create-content")!,from=document.querySelector<HTMLInputElement>("#mission-create-from")!,to=document.querySelector<HTMLInputElement>("#mission-create-to")!,submit=form.querySelector<HTMLButtonElement>('button[type="submit"]')!;const sync=()=>{to.min=from.value;submit.disabled=!title.value.trim()||!content.value.trim()||!from.value||!to.value||to.value<from.value||!form.checkValidity()};form.addEventListener("input",sync);form.addEventListener("change",sync);sync()}).observe(document.body,{childList:true,subtree:true});
async function openApplicants(id:number,title:string){document.querySelector("#mission-applicants-modal")?.remove();document.body.insertAdjacentHTML("beforeend",`<div id="mission-applicants-modal" class="priest-modal"><div class="priest-modal-backdrop" data-applicants-close></div><section class="priest-modal-box mission-applicants-box"><header><div><p>MISSION APPLICANTS</p><h2>${esc(title)} 지원자 현황</h2><small id="mission-applicants-summary">불러오는 중...</small></div><button type="button" data-applicants-close>×</button></header><div id="mission-applicants-content" class="mission-applicants-content"></div><footer><button class="secondary" data-applicants-close type="button">닫기</button></footer></section></div>`);document.body.classList.add("modal-open");const close=()=>{document.querySelector("#mission-applicants-modal")?.remove();document.body.classList.remove("modal-open")};document.querySelectorAll("[data-applicants-close]").forEach(el=>el.addEventListener("click",close));try{const result=await api<{mission:{authorType:string};items:Array<{id:number;name:string;baptismalName:string|null;mobile:string;email:string;message:string;status:"requested"|"approved"|"rejected";rejectionReason:string|null;appliedAt:string;approvedAt:string|null}>}>(`/api/parish/missions/${id}/applicants`);document.querySelector("#mission-applicants-summary")!.textContent=`지원자 ${result.items.length}명`;document.querySelector("#mission-applicants-content")!.innerHTML=result.items.length?`<div class="mission-applicants-grid"><table><thead><tr><th>이름</th><th>세례명</th><th>연락처</th><th>지원일</th><th>승인일</th><th>지원 메시지</th><th>상태</th><th>결정</th></tr></thead><tbody>${result.items.map(item=>`<tr><td><strong>${esc(item.name)}</strong></td><td>${esc(item.baptismalName??"-")}</td><td>${esc(item.mobile??"-")}<br>${esc(item.email)}</td><td class="applicant-date">${esc(item.appliedAt)}</td><td class="applicant-date">${esc(item.approvedAt??"-")}</td><td class="applicant-message">${esc(item.message||"-")}${item.rejectionReason?`<div class="admin-application-reason"><strong>반려 사유</strong><p>${esc(item.rejectionReason)}</p></div>`:""}</td><td>${item.status==="requested"?"결정 대기":item.status==="approved"?"승낙":"반려"}</td><td>${result.mission.authorType==="parish"&&item.status==="requested"?`<div class="applicant-decisions"><button data-admin-application="${item.id}" data-status="rejected" type="button">반려</button><button data-admin-application="${item.id}" data-status="approved" type="button">승낙</button></div>`:"-"}</td></tr>`).join("")}</tbody></table></div>`:'<div class="sharing-empty">지원자가 없습니다.</div>';document.querySelectorAll<HTMLButtonElement>("[data-admin-application]").forEach(button=>button.onclick=async()=>{const status=button.dataset.status,rejectionReason=status==="rejected"?await promptAdminApplicationRejection():"";if(status==="rejected"&&rejectionReason===null)return;await api(`/api/parish/mission-applications/${button.dataset.adminApplication}/decision`,{method:"PATCH",body:JSON.stringify({status,rejectionReason})});close();await load()})}catch(error){document.querySelector("#mission-applicants-content")!.textContent=(error as Error).message}}
async function openAdminQuestions(id:number,title:string){document.querySelector("#mission-questions-modal")?.remove();document.body.insertAdjacentHTML("beforeend",`<div id="mission-questions-modal" class="priest-modal"><div class="priest-modal-backdrop" data-admin-questions-close></div><section class="priest-modal-box mission-questions-box"><header><div><p>MISSION Q&amp;A</p><h2>${esc(title)} 질문·답변</h2></div><button type="button" data-admin-questions-close>×</button></header><div id="mission-admin-questions" class="mission-admin-questions">불러오는 중...</div><footer><button class="secondary" data-admin-questions-close type="button">닫기</button></footer></section></div>`);document.body.classList.add("modal-open");const close=()=>{document.querySelector("#mission-questions-modal")?.remove();document.body.classList.remove("modal-open")};document.querySelectorAll("[data-admin-questions-close]").forEach(el=>el.addEventListener("click",close));try{const result=await api<{mission:{author_type:string};items:Array<{id:number;question:string;answer:string|null;askerName:string;baptismalName:string|null;createdAt:string}>}>(`/api/parish/missions/${id}/questions`),root=document.querySelector("#mission-admin-questions")!;root.innerHTML=result.items.length?result.items.map(item=>`<article><header><strong>${esc(item.askerName)}${item.baptismalName?` (${esc(item.baptismalName)})`:""}</strong><time>${new Date(item.createdAt).toLocaleString("ko-KR")}</time></header><p>${esc(item.question)}</p>${item.answer?`<div class="admin-answer"><b>성당 답변</b><p>${esc(item.answer)}</p></div>`:result.mission.author_type==="parish"?`<form data-admin-answer="${item.id}"><textarea maxlength="5000" required placeholder="답변을 입력해 주세요."></textarea><button class="primary" type="submit">답변 등록</button></form>`:'<small>신도 등록자의 답변을 기다리고 있습니다.</small>'}</article>`).join(""):'<div class="sharing-empty">등록된 질문이 없습니다.</div>';root.querySelectorAll<HTMLFormElement>("[data-admin-answer]").forEach(form=>form.onsubmit=async event=>{event.preventDefault();await api(`/api/parish/mission-questions/${form.dataset.adminAnswer}/answer`,{method:"PATCH",body:JSON.stringify({answer:form.querySelector("textarea")!.value})});await openAdminQuestions(id,title)})}catch(error){document.querySelector("#mission-admin-questions")!.textContent=(error as Error).message}}
type MissionEditRequest={id:number;missionId:number;title:string;content:string;tags:string[];applicationFrom:string;applicationTo:string;requestedAt:string;authorName:string;baptismalName:string|null};
let loadingMissionEditRequests=false;
async function decorateMissionEditRequests(){const list=document.querySelector<HTMLElement>("#mission-admin-list");if(!list||loadingMissionEditRequests||list.querySelector(".mission-edit-request-card"))return;loadingMissionEditRequests=true;try{const items=await api<MissionEditRequest[]>("/api/parish/mission-edit-requests");items.reverse().forEach(item=>list.insertAdjacentHTML("afterbegin",`<article class="mission-admin-card mission-edit-request-card"><header><div><b class="mission-status requested">수정 승인 대기</b><strong>${esc(item.title)}</strong></div><time>${new Date(item.requestedAt).toLocaleString("ko-KR")}</time></header><dl><dt>작성자</dt><dd>${esc(item.authorName)}${item.baptismalName?` (${esc(item.baptismalName)})`:""}</dd><dt>수정 내용</dt><dd>${esc(item.content)}</dd><dt>모집기간</dt><dd>${item.applicationFrom} ~ ${item.applicationTo}</dd><dt>태그</dt><dd class="mission-tags">${item.tags.map(tag=>`<span>#${esc(tag)}</span>`).join("")||"-"}</dd></dl><footer><button class="secondary" data-edit-reject="${item.id}" type="button">반려</button><button class="primary" data-edit-approve="${item.id}" type="button">승인 및 반영</button></footer></article>`));list.querySelectorAll<HTMLButtonElement>("[data-edit-approve]").forEach(button=>button.onclick=()=>decideMissionEdit(Number(button.dataset.editApprove),"approved"));list.querySelectorAll<HTMLButtonElement>("[data-edit-reject]").forEach(button=>button.onclick=async()=>{const reason=await promptAdminApplicationRejection();if(reason!==null)await decideMissionEdit(Number(button.dataset.editReject),"rejected",reason)})}finally{loadingMissionEditRequests=false}}
async function decideMissionEdit(id:number,status:"approved"|"rejected",rejectionReason=""){const result=await api<{message:string}>(`/api/parish/mission-edit-requests/${id}/decision`,{method:"PATCH",body:JSON.stringify({status,rejectionReason})});await load();window.dispatchEvent(new CustomEvent("parish:notice",{detail:result.message}))}
const missionListForFilter=document.querySelector<HTMLElement>("#mission-admin-list")!;
let missionListRefreshQueued=false;
new MutationObserver(()=>{if(missionListRefreshQueued)return;missionListRefreshQueued=true;queueMicrotask(()=>{missionListRefreshQueued=false;void decorateMissionEditRequests();applyMissionStatusFilter()})}).observe(missionListForFilter,{childList:true});
queueMicrotask(()=>void decorateMissionEditRequests());
